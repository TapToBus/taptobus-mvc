<IfModule mod_rewrite.c>
  RewriteEngine on
  RewriteRule ^$ public/ [L]
  RewriteRule (.*) public/$1 [L]
</IfModule>

# enables to use -> localhost/taptobus/
# insted of -> localhost/taptobus/pages/index